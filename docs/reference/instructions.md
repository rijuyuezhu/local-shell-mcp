# MCP server instructions

This page is rendered from [`generated/server-instructions.json`](generated/server-instructions.json), which is generated from the MCP server instruction string by the same reference-data exporter that reads the live tool registry.

The instruction block is what `local-shell-mcp` advertises to MCP clients. Tool descriptions are rendered separately in the [Tools reference](tools.md), so this page does not repeat them.

<div class="generated-reference" data-reference-json="../generated/server-instructions.json">
Loading generated MCP server instructions...
</div>
